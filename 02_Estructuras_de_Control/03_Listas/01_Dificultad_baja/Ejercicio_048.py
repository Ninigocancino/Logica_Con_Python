# Crea una lista llamada pares que contiene los valores 2, 4, 6, 8, 10. Luego, inicia un bucle que itera sobre cada elemento de la lista pares, asignando el valor actual a la variable numero. En cada iteración, se imprime en la consola el resultado de multiplicar numero por 2.

pares = [2,4,6,8,10]
for numero in pares:
    print(numero * 2)